spring:
  application:
    name: question_search
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/lees_03?serverTimezone=Asia/Shanghai
    username: root
    password: root
  # redis 配置
  redis:
    port: 6379
    host: localhost
    database: 1
server:
  port: 8080

